import { makeStyles } from "@material-ui/core/styles";

export const useStyle = makeStyles({
  parent: {
    display: "flex",
    flexDirection: "column",
    width: "70%",
    height: "100vh",
    gap: 15,
  },
});
