import { makeStyles } from "@material-ui/core/styles";

export const useStyle = makeStyles({
  root: {
    width: "30%",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    padding: "0 20px",
    gap: 25,
    height: "100vh",
  },
});
